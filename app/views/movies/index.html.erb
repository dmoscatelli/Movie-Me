<% if !user_signed_in? %>
  <div class="jumbotron">
    <h1>Your Favorite Movies Homie</h1>
    <p>Come checkout your favorite movies and directors here in the sample app that means nothing at all. It will help you decide nothing, and most of all, check out a few not so great, but really great movies.</p>
    <p><%= link_to "Sign up to Write a Review", new_user_registration_path, class: "btn btn-primary btn-lg" %></p>
  </div>



<% end %>



<div class="row">
  <% @movies.each do |movie| %>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
        <%= link_to (image_tag movie.image.url(:medium), class: "image"), movie %>
      </div>
    </div>
  <% end %>
</div>